<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_app_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_app_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.GramaSanjeevini">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC6kmKft69Ed3mo4Sw_JjtgpLB-pyiUEyw" />

        <activity android:name=".activities.AddMedicineActivity" />
        <activity android:name=".activities.ExpiryAlertActivity" />
        <activity android:name=".activities.EmergencyStockActivity" />
        <activity android:name=".activities.PharmacistDashboardActivity" />
        <activity android:name=".activities.PharmacistSignupActivity" />
        <activity android:name=".activities.PharmacistLoginActivity" />
        <activity android:name=".activities.SearchMedicineActivity" />
        <activity android:name=".activities.MapActivity" />
        <activity android:name=".activities.MainActivity" />
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>